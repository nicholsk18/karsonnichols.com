---
import { cn } from "../../utils/helpers";
const { tag = "div", classList = "", ...rest } = Astro.props;
const TagName = tag;
---

<TagName class={cn("mx-4 md:mx-12 py-5", classList)} {...rest}>
  <slot />
</TagName>
